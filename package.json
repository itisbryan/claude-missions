{
  "name": "claude-missions",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-phase mission orchestration + git-worktree skills for Claude Code",
  "type": "module",
  "scripts": {
    "test": "node --test \"skills/mission/scripts/*.test.mjs\""
  }
}
